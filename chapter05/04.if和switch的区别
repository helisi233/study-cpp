判断条件角度
    switch只支持常量值固定相等的分支判断
    if还支持判断区间范围
    switch能做的if都能做 但反过来不行

分支角度
    分支数量少的时候if和switch差不多
    分支数量多
        if是跳转
            判断条件越多 形成的树形结构就深 如果多次没有命中性能会比switch结构要差
        switch是形成一张表
            不管判断条件有多少 查询效率不会降低 所有case的速度几乎都一样